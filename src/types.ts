export interface PropertyData {
  zip: string;
  bedrooms: number;
  bathrooms: number;
  squareMeters: number;
  yearBuilt: number;
}

export interface LeadData {
  name: string;
  email: string;
  phone: string;
}